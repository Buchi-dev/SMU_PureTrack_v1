# Staff Pages Component Visual Reference

## Component Anatomy

### PageHeader
```
┌─────────────────────────────────────────────────────────┐
│ ╭ Icon  Title                          Refresh  Action ╮ │  ← Header Card
│ │       Subtitle                       Button   Button  │ │
│ ╰─────────────────────────────────────────────────────────╯ │
└─────────────────────────────────────────────────────────┘
     Gradient background with Navy blue theme
```

**Usage**:
```tsx
<PageHeader
  icon={<BarChartOutlined />}
  title="Analytics"
  subtitle="Water quality trends"
  onRefresh={handleRefresh}
  actions={[{ label: 'Export', onClick: handleExport }]}
/>
```

---

### StatsCard
```
┌──────────────────────────┐
│┃ Icon  Metric Name  #    │  ← Color bar (left border)
│ 24.5  units              │  ← Value + Suffix
│ ━━━━━━━━━━━━━━━━━━━━━━━ │  ← Progress bar (optional)
│ Last 24 hours           │  ← Description
│ ↑ 5%                     │  ← Trend (optional)
└──────────────────────────┘
```

**Variations**:
- **Size**: Large (32px), Medium (28px), Small (20px)
- **Color**: Success (green), Warning (orange), Error (red), Info (blue)
- **Trend**: Up ↑ (green), Down ↓ (red), Neutral → (gray)

**Usage**:
```tsx
<Row gutter={[16, 16]}>
  <Col xs={24} sm={12} lg={8}>
    <StatsCard
      title="Online Devices"
      value={deviceStats.online}
      icon={<CheckCircleOutlined />}
      color={token.colorSuccess}
      progress={85}
      description="Currently active"
      trend="up"
      trendValue={5}
    />
  </Col>
</Row>
```

---

### PageContainer
```
┌─────────────────────────────────────────────────────┐
│                   Max Width: 1600px                │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │        Content Section (24px spacing)      │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │        Content Section (24px spacing)      │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
└─────────────────────────────────────────────────────┘
```

**States**:
- **Loading**: Shows skeleton loading
- **Empty**: Shows empty state message
- **Ready**: Shows content with consistent spacing

**Usage**:
```tsx
<PageContainer loading={loading} spacing="large">
  {/* Content automatically gets 24px spacing */}
</PageContainer>
```

---

### DataCard
```
┌──────────────────────────────────────────────────────┐
│ 📊 Card Title                          →  (Action)  │  ← Header
├──────────────────────────────────────────────────────┤
│                                                      │
│  Card Content Goes Here                            │
│  Can contain tables, charts, text, etc.            │
│                                                      │
├──────────────────────────────────────────────────────┤
│  Footer Content (optional)                         │  ← Footer
└──────────────────────────────────────────────────────┘
```

**Sizes**:
- **Large**: 24px padding
- **Medium**: 16px padding
- **Small**: 12px padding

**Usage**:
```tsx
<DataCard
  title="Recent Alerts"
  icon={<ThunderboltOutlined />}
  actionLabel="View All"
  onAction={() => navigate('/alerts')}
>
  <Table dataSource={alerts} columns={columns} />
</DataCard>
```

---

## Layout Patterns

### Dashboard Pattern
```
┌─────────────────────────────────────────┐
│          PageHeader                     │
├─────────────────────────────────────────┤
│  [StatsCard] [StatsCard] [StatsCard]   │
│  [StatsCard] [StatsCard] [StatsCard]   │  ← Grid: 3 or 4 columns
├─────────────────────────────────────────┤
│  ┌──────────────────┐  ┌──────────────┐ │
│  │   DataCard       │  │  DataCard    │ │  ← 12/12 or 16/8 split
│  │  (Wide)          │  │  (Sidebar)   │ │
│  │                  │  │              │ │
│  └──────────────────┘  └──────────────┘ │
├─────────────────────────────────────────┤
│       DataCard (Full Width)             │
└─────────────────────────────────────────┘
```

### Analytics Pattern
```
┌─────────────────────────────────────────┐
│          PageHeader                     │
├─────────────────────────────────────────┤
│  [StatsCard] [StatsCard] [StatsCard]   │  ← 3 columns
├─────────────────────────────────────────┤
│  [Chart 50%] [Chart 50%]               │  ← 2 columns
├─────────────────────────────────────────┤
│  [Chart 100%]                          │  ← Full width
├─────────────────────────────────────────┤
│  [Info Card 50%] [Info Card 50%]       │  ← 2 columns
└─────────────────────────────────────────┘
```

### Data Management Pattern
```
┌─────────────────────────────────────────┐
│          PageHeader                     │
├─────────────────────────────────────────┤
│  [Stat] [Stat] [Stat] [Stat]           │  ← Stats row
├─────────────────────────────────────────┤
│  [Filters Row]                         │  ← Search + Filters
├─────────────────────────────────────────┤
│         DataCard                        │
│    [Full Width Table]                  │  ← Table with pagination
│         (with actions)                  │
└─────────────────────────────────────────┘
```

---

## Color Usage Guide

### Status Colors
```
┌─────────────────┬──────────────┬─────────────────────┐
│ Severity        │ Color        │ Usage               │
├─────────────────┼──────────────┼─────────────────────┤
│ Success/Online  │ #52c41a 🟢  │ Online, Normal, OK  │
│ Warning/Caution │ #faad14 🟡  │ Alert, Warning      │
│ Error/Offline   │ #ff4d4f 🔴  │ Offline, Critical   │
│ Info/Neutral    │ #1890ff 🔵  │ General, Info       │
└─────────────────┴──────────────┴─────────────────────┘
```

### Usage Examples
```tsx
// Online Device
<StatsCard color={token.colorSuccess} />
✅ Green border + Green text

// Warning Alert
<StatsCard color={token.colorWarning} />
⚠️ Orange border + Orange text

// Offline Device
<StatsCard color={token.colorError} />
❌ Red border + Red text

// General Information
<StatsCard color={token.colorInfo} />
ℹ️ Blue border + Blue text
```

---

## Responsive Behavior

### StatsCard Grid
```
Desktop (lg+)        │ Tablet (sm-md)     │ Mobile (xs)
─────────────────────┼────────────────────┼──────────────
[C1] [C2] [C3] [C4] │ [C1] [C2]          │ [C1]
                     │ [C3] [C4]          │ [C2]
4 columns per row    │ 2 columns per row  │ 1 column per row
20% width each       │ 50% width each     │ 100% width
```

### Layout Splits
```
Desktop (xl)         │ Tablet/Laptop      │ Mobile
─────────────────────┼────────────────────┼──────────────
[Wide 2/3] [Side 1/3]│ [Full Width]       │ [Full Width]
                     │ [Full Width]       │ [Full Width]
Column layout        │ Stacked            │ Stacked
```

---

## Icon Reference

### Page Icons
```
Dashboard      → <DashboardOutlined />
Devices        → <ApiOutlined />
Analytics      → <BarChartOutlined />
Readings       → <LineChartOutlined />
Settings       → <SettingOutlined />
```

### Status Icons
```
Online         → <CheckCircleOutlined />  (Green)
Offline        → <ClockCircleOutlined />  (Gray)
Warning        → <WarningOutlined />      (Orange)
Alert          → <AlertOutlined />        (Red)
Info           → <InfoCircleOutlined />   (Blue)
```

### Action Icons
```
View           → <EyeOutlined />
Refresh        → <ReloadOutlined />
Export         → <DownloadOutlined />
Add            → <PlusOutlined />
Delete         → <DeleteOutlined />
Edit           → <EditOutlined />
Search         → <SearchOutlined />
Next           → <ArrowRightOutlined />
```

---

## Spacing Reference

### Page Level
```
┌─────────────────────────────────────────┐
│                                         │  24px padding
│     Page Content (max-width: 1600px)   │
│                                         │
└─────────────────────────────────────────┘
```

### Component Level
```
Row Gutter:      16px horizontal + 16px vertical
Card Padding:    16-24px (depends on size)
Section Gap:     24px between major sections
Component Gap:   8-16px between sub-components
```

### Visual Example
```
┌──────────────────────────────────────┐
│         PageHeader (Card)             │  ← 24px top margin
├──────────────────────────────────────┤
                 ↓ 24px
├──────────────────────────────────────┤
│  [Stat] 16px [Stat] 16px [Stat]     │  ← 16px gutter
├──────────────────────────────────────┤
                 ↓ 24px
├──────────────────────────────────────┤
│     DataCard                          │
│  ┌────────────────────────────────┐  │
│  │ Content     16px padding       │  │
│  └────────────────────────────────┘  │
└──────────────────────────────────────┘
```

---

## Quick Copy-Paste Examples

### Basic Dashboard Page
```tsx
import { PageHeader, StatsCard, PageContainer, DataCard } from '../../../components/staff';

export const MyPage = () => {
  const token = useThemeToken();
  
  return (
    <StaffLayout>
      <PageContainer spacing="large">
        <PageHeader 
          title="My Page"
          icon={<DashboardOutlined />}
          onRefresh={() => window.location.reload()}
        />
        
        <Row gutter={[16, 16]}>
          <Col xs={24} sm={12} lg={6}>
            <StatsCard title="Total" value={100} color={token.colorInfo} />
          </Col>
          <Col xs={24} sm={12} lg={6}>
            <StatsCard title="Online" value={85} color={token.colorSuccess} />
          </Col>
        </Row>
        
        <DataCard title="Data">
          {/* Your content here */}
        </DataCard>
      </PageContainer>
    </StaffLayout>
  );
};
```

---

**Created**: October 2025  
**Version**: 1.0  
**Status**: Active  
**Maintenance**: Keep updated with design changes
