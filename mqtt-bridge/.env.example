# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  🔐 REQUIRED CREDENTIALS (SENSITIVE - DO NOT COMMIT .env FILE)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# MQTT Broker Credentials
MQTT_BROKER_URL=mqtts://example.s1.eu.hivemq.cloud:8883
MQTT_USERNAME=example_user
MQTT_PASSWORD=example_password

# Google Cloud Project ID
GCP_PROJECT_ID=example_id

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  ⚙️ OPTIONAL SETTINGS
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Server Port (default: 8080)
PORT=8080

# Logging Level (default: info) - Options: trace, debug, info, warn, error, fatal
# IMPORTANT: Use 'info' for production to maintain operational visibility
LOG_LEVEL=info

NODE_ENV=production

# CORS Allowed Origins (REQUIRED - comma-separated, NO spaces)
# Production example:
ALLOWED_ORIGINS=https://my-app-da530.web.app,https://my-app-da530.firebaseapp.com,http://localhost:5173,http://127.0.0.1:5173

# Development example:
# ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  ⚠️ IMPORTANT NOTES
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 1. DATA LOSS WARNING: This bridge uses in-memory buffering with no
#    persistence layer. On crash/restart, buffered messages are lost.
#    Maximum potential loss: ~100 messages per topic (5-second window).
#
# 2. All Pub/Sub topics, buffer settings, and monitoring thresholds are
#    hardcoded in the application for consistency across deployments.
#
# 3. ALLOWED_ORIGINS must be comma-separated with NO spaces between URLs.


