version: '3.9'

services:
  app:
    build: .
    container_name: server_app
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
      - .firebase-service-account.json
    # Using cloud-based MongoDB and Redis services
    # No local database containers needed
