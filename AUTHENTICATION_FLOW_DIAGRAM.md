# Authentication Flow - Visual Reference

## 🎯 Complete User Journey

```
┌──────────────────────────────────────────────────────────────────────┐
│                         USER JOURNEY DIAGRAM                          │
└──────────────────────────────────────────────────────────────────────┘

┌─────────────────┐
│   User visits   │
│  /auth/login    │
└────────┬────────┘
         │
         v
┌─────────────────────────────────┐
│  GoogleAuth Component           │
│  • Shows "Sign in with Google"  │
│  • User clicks button           │
└────────┬────────────────────────┘
         │
         v
┌─────────────────────────────────┐
│  Google OAuth Popup             │
│  • User selects account         │
│  • Google authenticates         │
└────────┬────────────────────────┘
         │
         v
┌─────────────────────────────────────────────────────────────────┐
│  FIREBASE AUTH - Cloud Functions (Server-Side)                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────────┐                │
│  │ FIRST TIME USER?                           │                │
│  │                                             │                │
│  │  YES → beforeCreate Function                │                │
│  │        ├─ Create user in Firestore          │                │
│  │        │  • uuid, firstname, lastname       │                │
│  │        │  • email, role: "Staff"            │                │
│  │        │  • status: "Pending"               │                │
│  │        ├─ Log to business_logs              │                │
│  │        └─ Allow creation ✓                  │                │
│  │                                             │                │
│  │  NO  → Skip to beforeSignIn                 │                │
│  └────────────────────────────────────────────┘                │
│                     ↓                                           │
│  ┌────────────────────────────────────────────┐                │
│  │ beforeSignIn Function (ALWAYS RUNS)         │                │
│  │                                             │                │
│  │  1. Get user from Firestore                 │                │
│  │  2. Check status field                      │                │
│  │  3. Log attempt to login_logs               │                │
│  │  4. Decision Tree:                          │                │
│  │                                             │                │
│  │  ┌─────────────────────────────┐           │                │
│  │  │ status = "Pending"?          │           │                │
│  │  │  YES → REJECT ✗              │           │                │
│  │  │  Error: "Account pending"    │           │                │
│  │  └─────────────────────────────┘           │                │
│  │                                             │                │
│  │  ┌─────────────────────────────┐           │                │
│  │  │ status = "Suspended"?        │           │                │
│  │  │  YES → REJECT ✗              │           │                │
│  │  │  Error: "Account suspended"  │           │                │
│  │  └─────────────────────────────┘           │                │
│  │                                             │                │
│  │  ┌─────────────────────────────┐           │                │
│  │  │ status = "Approved"?         │           │                │
│  │  │  YES → ALLOW ✓               │           │                │
│  │  │  Update lastLogin            │           │                │
│  │  └─────────────────────────────┘           │                │
│  │                                             │                │
│  └────────────────────────────────────────────┘                │
└─────────────────────────────────────────────────────────────────┘
         │
         │ Auth Token Issued (if approved)
         v
┌─────────────────────────────────────────────────────────────────┐
│  CLIENT-SIDE ROUTING (GoogleAuth Component)                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Get user document from Firestore                               │
│  Check: department + phoneNumber filled?                        │
│  Check: status field                                            │
│                                                                  │
│  ┌──────────────────────────────────────────┐                  │
│  │ Missing dept/phone?                       │                  │
│  │  YES → /auth/complete-account             │                  │
│  │        └─ AccountCompletion Component     │                  │
│  └──────────────────────────────────────────┘                  │
│                     ↓                                           │
│  ┌──────────────────────────────────────────┐                  │
│  │ status = "Pending"?                       │                  │
│  │  YES → /auth/pending-approval             │                  │
│  │        └─ PendingApproval Component       │                  │
│  │           • Shows waiting screen          │                  │
│  │           • Real-time status listener     │                  │
│  │           • Auto-redirect when approved   │                  │
│  └──────────────────────────────────────────┘                  │
│                     ↓                                           │
│  ┌──────────────────────────────────────────┐                  │
│  │ status = "Suspended"?                     │                  │
│  │  YES → /auth/account-inactive             │                  │
│  │        └─ AccountInactive Component       │                  │
│  │           • Shows suspension notice       │                  │
│  │           • Contact admin button          │                  │
│  └──────────────────────────────────────────┘                  │
│                     ↓                                           │
│  ┌──────────────────────────────────────────┐                  │
│  │ status = "Approved"?                      │                  │
│  │  YES → Check role                         │                  │
│  │     ├─ Admin → /admin/dashboard           │                  │
│  │     └─ Staff → /dashboard                 │                  │
│  └──────────────────────────────────────────┘                  │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 State Transitions

```
┌──────────────┐
│   NEW USER   │
│  (No account)│
└──────┬───────┘
       │
       │ First sign-in
       v
┌──────────────────┐      Fill profile      ┌──────────────────┐
│  PROFILE EMPTY   │ ──────────────────────> │ PROFILE FILLED   │
│ status: Pending  │                         │ status: Pending  │
└──────────────────┘                         └────────┬─────────┘
                                                      │
                                         Admin action │
                                                      v
                                             ┌──────────────────┐
                                             │    APPROVED      │
                                             │ status: Approved │
                                             └────────┬─────────┘
                                                      │
                                         Can sign in  │
                                         and access   │
                                         dashboard    v
                                             ┌──────────────────┐
                                             │   ACTIVE USER    │
                                             │  Full Access     │
                                             └──────────────────┘

                                        Admin can suspend:
                                             ┌──────────────────┐
                                             │   SUSPENDED      │
                                             │status: Suspended │
                                             └──────────────────┘
                                                      │
                                        Sign-in blocked
                                        Cannot access system
```

---

## 📊 Firestore Data Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    FIRESTORE COLLECTIONS                         │
└─────────────────────────────────────────────────────────────────┘

users Collection
─────────────────
{userId}
  ├─ uuid: string
  ├─ firstname: string
  ├─ lastname: string
  ├─ middlename: string
  ├─ department: string
  ├─ phoneNumber: string
  ├─ email: string
  ├─ role: "Staff" | "Admin"
  ├─ status: "Pending" | "Approved" | "Suspended"  ← KEY FIELD
  ├─ createdAt: Timestamp
  ├─ updatedAt?: Timestamp
  └─ lastLogin?: Timestamp

login_logs Collection
─────────────────────
{logId}
  ├─ uid: string
  ├─ email: string
  ├─ displayName: string
  ├─ statusAttempted: "Pending" | "Approved" | "Suspended"
  ├─ timestamp: Timestamp
  ├─ result: "success" | "rejected" | "error"
  └─ message: string

business_logs Collection
────────────────────────
{logId}
  ├─ action: "user_created" | "user_approved" | "user_suspended"
  ├─ uid: string
  ├─ email: string
  ├─ performedBy: string (uid or "system")
  ├─ timestamp: Timestamp
  └─ details: object
```

---

## 🎬 Timeline of Events

### New User Registration
```
T=0s    │ User clicks "Sign in with Google"
T=1s    │ Google OAuth popup appears
T=3s    │ User selects account and authorizes
T=4s    │ ─────────────────────────────────────────────
        │ SERVER-SIDE (Cloud Functions)
        │ ─────────────────────────────────────────────
        │ beforeCreate function triggered
        │   ├─ Create user document in Firestore
        │   │  • status: "Pending"
        │   │  • role: "Staff"
        │   └─ Log to business_logs
        │
        │ beforeSignIn function triggered
        │   ├─ Read user document
        │   ├─ Check status = "Pending"
        │   ├─ Log to login_logs (result: "rejected")
        │   └─ REJECT with error message
        │ ─────────────────────────────────────────────
        │ CLIENT RECEIVES ERROR
        │ ─────────────────────────────────────────────
T=5s    │ Error caught in GoogleAuth component
T=6s    │ User document exists but profile incomplete
T=7s    │ → Redirect to /auth/complete-account
        │
T=10s   │ User fills department & phone number
T=11s   │ Update Firestore user document
T=12s   │ → Redirect to /auth/pending-approval
        │
        │ User sees: "Account Pending Approval"
        │ Real-time listener active on user document
        │
...     │ [Waiting for admin approval]
        │
T=300s  │ Admin changes status to "Approved"
T=301s  │ Real-time listener detects change
T=302s  │ → Auto-redirect to /admin/dashboard
```

### Approved User Sign-in
```
T=0s    │ User clicks "Sign in with Google"
T=1s    │ Google OAuth (faster - already authorized)
T=2s    │ ─────────────────────────────────────────────
        │ SERVER-SIDE (Cloud Functions)
        │ ─────────────────────────────────────────────
        │ beforeSignIn function triggered
        │   ├─ Read user document
        │   ├─ Check status = "Approved" ✓
        │   ├─ Update lastLogin timestamp
        │   ├─ Log to login_logs (result: "success")
        │   └─ ALLOW sign-in
        │ ─────────────────────────────────────────────
        │ AUTH TOKEN ISSUED
        │ ─────────────────────────────────────────────
T=3s    │ Client receives auth token
T=4s    │ GoogleAuth component checks user data
T=5s    │ Profile complete + status approved
T=6s    │ → Redirect to /admin/dashboard
        │
        │ ✓ User successfully signed in
```

---

## 🔐 Security Checkpoints

```
┌─────────────────────────────────────────────────────────────┐
│  CHECKPOINT 1: Google OAuth                                  │
│  • Verifies Google account                                   │
│  • Issues OAuth token                                        │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│  CHECKPOINT 2: beforeCreate (First time only)               │
│  • Can reject based on domain/IP                            │
│  • Creates user with default "Pending" status               │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│  CHECKPOINT 3: beforeSignIn (Every sign-in)                 │
│  • Checks Firestore user status                             │
│  • Blocks "Pending" and "Suspended" users                   │
│  • Only allows "Approved" users                             │
│  • Logs all attempts                                        │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│  CHECKPOINT 4: Client-Side Routing                          │
│  • Verifies profile completion                              │
│  • Routes based on status                                   │
│  • Real-time monitoring of status changes                   │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│  CHECKPOINT 5: Firestore Security Rules (Recommended)       │
│  • Validates data writes                                    │
│  • Enforces role-based access                               │
│  • Protects sensitive fields                                │
└─────────────────────────────────────────────────────────────┘
```

---

## 📈 Admin Workflow

```
┌──────────────────────────────────────────────────────────────┐
│                    ADMIN APPROVAL PROCESS                     │
└──────────────────────────────────────────────────────────────┘

1. New User Registers
   └─ Notification (optional): New user pending approval

2. Admin Reviews User
   Firebase Console → Firestore → users collection
   
   User Document:
   ┌────────────────────────────────────┐
   │ uuid: "abc123"                     │
   │ email: "john@example.com"          │
   │ firstname: "John"                  │
   │ lastname: "Doe"                    │
   │ department: "Engineering"          │
   │ phoneNumber: "+1234567890"         │
   │ role: "Staff"                      │
   │ status: "Pending"  ← CHANGE THIS  │
   │ createdAt: Timestamp               │
   └────────────────────────────────────┘

3. Admin Takes Action
   
   Option A: APPROVE
   └─ Change status to "Approved"
      User can now sign in

   Option B: SUSPEND
   └─ Change status to "Suspended"
      User cannot sign in

   Option C: REJECT (Delete)
   └─ Delete user document
      User must re-register

4. User Notified (optional)
   └─ Email/SMS notification of status change

5. User Signs In
   └─ If approved: Access granted
   └─ If suspended: Error shown
```

---

## 🎯 Key Points

1. **Server-Side First**: All validation happens in Cloud Functions before tokens are issued
2. **Real-Time Updates**: Client listens for status changes via Firestore real-time listeners
3. **Comprehensive Logging**: Every sign-in attempt is logged with result
4. **User-Friendly**: Clear feedback at every step
5. **Secure**: Cannot bypass approval workflow
6. **Scalable**: Cloud Functions auto-scale with demand

---

**For detailed implementation steps, see:** `QUICK_START.md`  
**For complete documentation, see:** `AUTHENTICATION_IMPLEMENTATION_GUIDE.md`
